<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Talking Therapies Research Resource</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="">
      <header class="header bg-near-white pa4">
        <nav role="navigation">
          <div class="dt dt--fixed w-100">
            <%= link "Home", to: page_path(@conn, :index), class: "dtc tc ttu no-underline blue" %>
            <%= link "About", to: about_path(@conn, :index), class: "dtc tc ttu no-underline blue" %>
            <%= link "Projects", to: projects_path(@conn, :index), class: "dtc tc ttu no-underline blue" %>
            <%= link "Who", to: who_we_are_path(@conn, :index), class: "dtc tc ttu no-underline blue" %>
            <%= link "FAQS", to: faqs_path(@conn, :index), class: "dtc tc ttu no-underline blue" %>
            <%= if @current_user do %>
              <div class="dtc tr">
                <span><%=String.capitalize @current_user.first_name%></span>
                <span>|</span>
                <%= link "My Account", to: account_path(@conn, :index), class: "no-underline blue" %>
              </div>
            <% else %>
            <div class="dtc tr">
              <%= link "Sign Up", to: user_path(@conn, :new), class: "no-underline blue ttu"  %>
              <span>|</span>
              <%= link "Log In", to: session_path(@conn, :new), class: "no-underline blue ttu"  %>
            </div>
            <% end %>
          </div>
        </nav>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
